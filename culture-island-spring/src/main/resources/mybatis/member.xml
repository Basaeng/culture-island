<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.cultureisland.member.model.MemberMapper">

    <select id="findAll" resultType="HashMap">
        SELECT * FROM member;
    </select>

    <insert id="joinMember" parameterType="MemberDTO">
        INSERT INTO member (email, password, name, number, birth, gender, rank, imglink, intro)
        VALUES (#{email}, #{password}, #{name}, #{number}, #{birth}, #{gender}, #{rank}, #{imglink}, #{intro})
    </insert>

</mapper>
