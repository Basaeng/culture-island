<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.cultureisland.board.model.BoardMapper">
    <resultMap type="boardDto" id="article">
        <result column="id" property="articleNo"/>
        <result column="member_id" property="memberId"/>
        <result column="name" property="name"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="hit" property="hit"/>
        <result column="register_time" property="registerTime"/>
    </resultMap>

    <sql id="search">
        <if test="word != null and word != ''">
            <if test="key == 'subject'">
                and subject like concat('%', #{word}, '%')
            </if>
            <if test="key != 'subject'">
                and ${key} = #{word}
            </if>
        </if>
    </sql>

    <select id="listArticle" parameterType="map" resultMap="article">
        select b.id, b.member_id, b.name, b.subject, b.content, b.hit, b.register_time
        from article b, member m
        where b.name = m.name
        <include refid="search"/>
        order by b.id desc
        limit #{start}, #{listSize}
    </select>

    <select id="getTotalArticleCount" parameterType="map" resultType="int">
        select count(id)
        from article
        <where>
            <include refid="search"/>
        </where>
    </select>

    <insert id="writeArticle" parameterType="boardDto">
        insert into article (member_id, name, subject, content, hit, register_time)
        values (#{memberId}, #{name}, #{subject}, #{content}, 0, now())
        <selectKey resultType="int" keyProperty="articleNo" order="AFTER">
            select last_insert_id()
        </selectKey>
    </insert>

    <select id="getArticle" parameterType="int" resultMap="article">
        select id, member_id, name, subject, content, hit, register_time
        from article
        where id = #{articleNo}
    </select>

    <update id="updateHit" parameterType="int">
        update article
        set hit = hit + 1
        where id = #{articleNo}
    </update>

    <delete id="deleteArticle" parameterType="int">
        delete from article
        where id = #{articleNo}
    </delete>

</mapper>
